# 一期视觉约定

## 主题

延续暗色现场资料档案：近黑底、红色信号、紧凑标题和团体视觉。手机用于查团查活动，桌面用于比对资料；页面首先提供可操作的信息，不新增营销横幅。

## 色彩

沿用 app.css：画布 oklch(0.125 0.008 25)，一级表面 oklch(0.155 0.009 25)，二级表面 oklch(0.19 0.01 25)，正文 oklch(0.92 0.012 25)，次级正文 oklch(0.78 0.014 25)，强调 oklch(0.625 0.205 27)，链接 oklch(0.72 0.19 27)。红色表示可操作或选中，不用它单独传达取消状态。

## 字体

保留 Bahnschrift Condensed / Arial Narrow / Microsoft YaHei UI 标题栈，以及 Source Han Sans SC / Noto Sans CJK SC / Microsoft YaHei UI 正文栈。无需下载网络字体。数字用等宽数字特性，正文至少 14px 并保留充足行高。

## 组件

原生 CSS 是唯一样式策略。全局导航 site-nav；各页面局部样式使用 events/content 命名空间。动作 button，页面跳转 a；焦点使用原有高亮轮廓。圆角仅 4/8/12/pill，导航 4px。选中入口使用浅红表面和亮红文字，按压缩放 0.96，120ms，可减少动画。

## 布局

导航、口径、工作区、详情按阅读顺序排列。档案主图保持既有布局；活动日期与团体名称优先；内容页侧目录在窄屏进入正常文档流。间距沿用 4/8/12/16/24px。

## 层次

使用暗色表面明度差表示层次。细分隔线用于资料区，不为每段文字套卡片，不新增装饰渐变或玻璃模糊。

## 内容约束

历史截点、编辑时间、粉丝观察时间分别显示。空活动写“尚未收录”，日期经过不写“已举办”。源文本通过 textContent 显示。外链明确来源，邮件草稿明确未发送。

## 响应式

至少检查 375px 与 1440px。导航折行且每项至少 40px 高；不横向藏起关键动作。保留浏览器缩放与键盘焦点。减少动画时取消导航变换。

## 后续组件提示

- 添加资料入口：使用 var(--text-soft)，14px 正文、1.7 行高、16px 区块间距；链接 var(--accent-hot)，至少 40px 高，不新增字体。
- 添加选中状态：var(--accent-soft) 表面，var(--accent-hot) 文字，4px 圆角，120ms transform 过渡；减少动画时禁用变换。
- 添加日期摘要：日期使用等宽数字，标题 16px、600 字重，正文 14px、1.7 行高，背景 var(--surface-2)，12px 内边距。
