"use strict";
(() => {
  // data/events.v1.json
  var events_v1_default = {
    schemaVersion: "idol-events-v1",
    updatedAt: "2026-09-08T08:36:19Z",
    coverage: "partial",
    events: [
      {
        id: "e-weibo-5338755200452940",
        title: "偶像回响 Idol Echo Live Vol.31 · 小丸mori生诞祭",
        date: "2026-09-05",
        province: null,
        city: null,
        venue: "VeinLab未来俱乐部",
        address: null,
        opensAt: null,
        startsAt: null,
        endsAt: null,
        status: "scheduled",
        performers: [{ groupId: "g263", name: "TakeBlue" }],
        sources: [
          {
            url: "https://weibo.com/detail/5338755200452940",
            label: "TakeBlue 本周演出情报",
            publisher: "TakeBlue 官号",
            observedAt: "2026-09-03T11:43:48.400Z",
            kind: "official"
          }
        ],
        notes: "据已归档官号预告整理，未实时复核变更。预告中的 15:40–16:00 为 TakeBlue 出演时段，不能据此确定整场开演时间；仅收录已核实的部分阵容。省市、完整地址和整场时刻未确认。",
        poster: null
      },
      {
        id: "e-weibo-5339142341002069",
        title: "SIN RETORNO 世界树剧场演出",
        date: "2026-09-06",
        province: null,
        city: null,
        venue: "世界树剧场",
        address: null,
        opensAt: null,
        startsAt: null,
        endsAt: null,
        status: "scheduled",
        performers: [{ groupId: "g032", name: "SIN RETORNO" }],
        sources: [
          {
            url: "https://weibo.com/detail/5339142341002069",
            label: "SIN RETORNO 本周演出预告",
            publisher: "SIN RETORNO 官号",
            observedAt: "2026-09-03T11:46:52.435Z",
            kind: "official"
          }
        ],
        notes: "标题为依据官宣整理的描述性标题，整场活动正式名称未确认。预告中的 18:20–18:40 为本团出演时段，整场开演时间留空。省市及地址未确认；未实时复核取消、延期或阵容变更。",
        poster: null
      },
      {
        id: "e-weibo-5339009950681922",
        title: "紫禁之巅 Vol.2 —— 京门第一",
        date: "2026-09-06",
        province: "北京",
        city: "北京",
        venue: "门空间TheDoorSpace",
        address: "北京市西城区廊房头条2号院1号楼2层01号",
        opensAt: null,
        startsAt: null,
        endsAt: null,
        status: "scheduled",
        performers: [
          { groupId: "g192", name: "PokaPokaTime" },
          { groupId: "g190", name: "風時計Kazetoke" }
        ],
        sources: [
          {
            url: "https://weibo.com/detail/5339009950681922",
            label: "PokaPokaTime 本周活动预告",
            publisher: "PokaPokaTime 官号",
            observedAt: "2026-09-03T11:46:52.435Z",
            kind: "official"
          },
          {
            url: "https://weibo.com/detail/5339081194342085",
            label: "風時計Kazetoke 本周课程安排",
            publisher: "風時計Kazetoke 官号",
            observedAt: "2026-09-03T11:43:48.400Z",
            kind: "official"
          }
        ],
        notes: "日期和活动全名来自 PokaPokaTime 预告，城市及完整地址由同场活动的風時計Kazetoke 官号预告交叉核对。仅列已核实的部分出演团体，整场入场、开演和结束时刻未确认；未实时复核变更。",
        poster: null
      },
      {
        id: "e-weibo-5338640013852929",
        title: "MOGUMOGU生长计划4 · RED²红色的二次方",
        date: "2026-09-13",
        province: "内蒙古",
        city: "呼和浩特",
        venue: null,
        address: null,
        opensAt: "13:40",
        startsAt: "14:00",
        endsAt: null,
        status: "scheduled",
        performers: [
          { groupId: "g185", name: "绯色苏打" },
          { groupId: null, name: "MOGUMOGU" }
        ],
        sources: [
          {
            url: "https://weibo.com/detail/5338640013852929",
            label: "绯色苏打 TWOMAN LIVE 官宣",
            publisher: "绯色苏打 官号",
            observedAt: "2026-09-03T11:46:52.435Z",
            kind: "official"
          },
          {
            url: "https://www.sina.cn/news/detail/5331060167541520.html",
            label: "绯色苏打 8 月 12 日预告（新浪公开页）",
            publisher: "绯色苏打_Official",
            observedAt: "2026-09-08T08:33:36Z",
            kind: "official"
          }
        ],
        notes: "归档正文明确活动日期、OPEN 13:40 与 START 14:00。2026-09-08 补核绯色苏打 8 月 12 日预告，同名活动及同日日期明确指向内蒙古呼和浩特；具体场地及地址仍未确认。MOGUMOGU 按官宣名称收录，未独立核验身份绑定。补核历史预告不表示已排除后续取消、延期或阵容变更。",
        poster: null
      },
      {
        id: "e-weibo-5339115526292485",
        title: "Meguri-Kaado Last one-man",
        date: "2026-09-15",
        province: "北京",
        city: "北京",
        venue: "门空间TheDoorSpace",
        address: null,
        opensAt: "18:45",
        startsAt: "19:00",
        endsAt: null,
        status: "scheduled",
        performers: [{ groupId: null, name: "Meguri-Kaado" }],
        sources: [
          {
            url: "https://weibo.com/detail/5339115526292485",
            label: "螢石FluorMelo 活动邀请转发",
            publisher: "螢石FluorMelo 官号",
            observedAt: "2026-09-03T11:47:14.413Z",
            kind: "official"
          },
          {
            url: "https://www.sina.cn/news/detail/5338725122835985.html",
            label: "Meguri-Kaado 9 月 2 日原预告（新浪公开页）",
            publisher: "Meguri-Kaado",
            observedAt: "2026-09-08T08:33:36Z",
            kind: "official"
          },
          {
            url: "https://www.sina.cn/news/detail/5334341322280544.html",
            label: "TrueWorld 场馆城市交叉依据（非本场排期）",
            publisher: "TrueWorld-",
            observedAt: "2026-09-08T08:33:36Z",
            kind: "organizer"
          }
        ],
        notes: "2026-09-08 补核 Meguri-Kaado 9 月 2 日原预告，确认日期、入场、开演及场地，与原邀请转发一致。结合 TrueWorld 其他场次的同场馆、区、院号、楼层资料交叉确认城市北京；该来源仅用于城市，不证明本场排期。街名存在廊坊头条与廊房头条异写，完整地址仍留空。主出演未绑定团体档案，未据致谢文案推断其他出演者。补核历史预告不表示已排除后续取消、延期或阵容变更。",
        poster: null
      }
    ]
  };

  // src/catalog/model.ts
  var ID_PATTERN = /^g\d{3,8}$/u;
  function hasControl(value, multiline = false) {
    return Array.from(value).some((character) => {
      const code = character.charCodeAt(0);
      return (code < 32 || code === 127) && !(multiline && [9, 10, 13].includes(code));
    });
  }
  var GROUP_KEYS = [
    "id",
    "name",
    "handle",
    "aliases",
    "status",
    "isActive",
    "founding",
    "activity",
    "styleLabel",
    "styleState",
    "styleNote",
    "tags",
    "avatar",
    "visual",
    "officialUrl",
    "wikiUrl",
    "followers",
    "sources"
  ];
  function object(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
  }
  function text(value, max, empty = false) {
    return typeof value === "string" && value.length <= max && (empty || value.trim().length > 0) && !hasControl(value);
  }
  function note(value) {
    return typeof value === "string" && value.length <= 8e3 && !hasControl(value, true);
  }
  function date(value) {
    if (typeof value !== "string" || !/^\d{4}-\d{2}-\d{2}$/u.test(value))
      return false;
    const parsed = /* @__PURE__ */ new Date(`${value}T00:00:00Z`);
    return Number.isFinite(parsed.getTime()) && parsed.toISOString().slice(0, 10) === value;
  }
  function observedAt(value) {
    if (value === null || date(value)) return true;
    if (typeof value !== "string") return false;
    const match = /^(\d{4}-\d{2}-\d{2})T([01]\d|2[0-3]):([0-5]\d):([0-5]\d)(?:\.\d{1,9})?(?:Z|[+-](?:0\d|1[0-4]):[0-5]\d)$/u.exec(
      value
    );
    return Boolean(match && date(match[1]) && Number.isFinite(Date.parse(value)));
  }
  function isSafeCatalogUrl(value) {
    if (typeof value !== "string" || value.length > 4096 || !/^https?:\/\//iu.test(value) || /\s|\\/u.test(value) || hasControl(value))
      return false;
    try {
      const decoded = decodeURIComponent(value);
      if (hasControl(decoded) || decoded.includes("\\")) return false;
      const url = new URL(value);
      return ["http:", "https:"].includes(url.protocol) && Boolean(url.hostname) && !url.username && !url.password;
    } catch {
      return false;
    }
  }
  function isLocalCatalogImagePath(value) {
    return typeof value === "string" && value.length <= 240 && /^assets\/(?:avatars|weibo-avatars|weibo-api-avatar-candidates|group-visuals|posters|profile-covers|weibo-cached-visuals)\/[a-z0-9_-]+\.(?:jpe?g|png|webp)$/u.test(
      value
    );
  }
  function validateCatalog(input) {
    const errors = [];
    const fail = (path, message) => {
      if (errors.length < 200) errors.push(`${path}: ${message}`);
    };
    const shape = (value, path, keys) => {
      if (!object(value)) {
        fail(path, "必须为对象");
        return false;
      }
      for (const key of Object.keys(value))
        if (!keys.includes(key)) fail(`${path}.${key}`, "不允许的字段");
      for (const key of keys)
        if (!Object.hasOwn(value, key)) fail(`${path}.${key}`, "缺少字段");
      return true;
    };
    const stringField = (value, path, max = 200, empty = false) => {
      if (!text(value, max, empty))
        fail(path, "文字为空、过长、类型错误或含控制字符");
    };
    const strings = (value, path) => {
      if (!Array.isArray(value) || value.length > 64) {
        fail(path, "必须为最多64项的文字数组");
        return;
      }
      Array.from(value).forEach(
        (item, index) => stringField(item, `${path}[${index}]`)
      );
      if (new Set(value).size !== value.length) fail(path, "数组包含重复项");
    };
    const region = (value, path) => {
      if (!shape(value, path, ["province", "city"])) return;
      for (const key of ["province", "city"])
        if (value[key] !== null) stringField(value[key], `${path}.${key}`, 80);
    };
    const url = (value, path, nullable = true) => {
      if (!(nullable && value === null) && !isSafeCatalogUrl(value))
        fail(path, "必须为安全的HTTP(S)链接");
    };
    const image = (value, path) => {
      if (value === null || !shape(value, path, ["src", "alt", "label", "sourceUrl"]))
        return;
      if (!isLocalCatalogImagePath(value.src))
        fail(`${path}.src`, "非法公开素材路径");
      stringField(value.alt, `${path}.alt`, 400);
      stringField(value.label, `${path}.label`, 400);
      url(value.sourceUrl, `${path}.sourceUrl`);
    };
    if (!shape(input, "catalog", ["schemaVersion", "archiveDate", "groups"]))
      return { valid: false, errors };
    if (input.schemaVersion !== "idol-catalog-v1")
      fail("catalog.schemaVersion", "不支持的索引版本");
    if (!date(input.archiveDate))
      fail("catalog.archiveDate", "必须为真实日历日期");
    if (!Array.isArray(input.groups) || input.groups.length > 2e4) {
      fail("catalog.groups", "必须为不超过20000项的数组");
      return { valid: false, errors };
    }
    const ids = /* @__PURE__ */ new Set();
    Array.from(input.groups).forEach((group, index) => {
      const path = `catalog.groups[${index}]`;
      if (!shape(group, path, GROUP_KEYS)) return;
      if (typeof group.id !== "string" || !ID_PATTERN.test(group.id))
        fail(`${path}.id`, "非法团体ID");
      else if (ids.has(group.id)) fail(`${path}.id`, "重复团体ID");
      else ids.add(group.id);
      for (const key of ["name", "handle", "status", "styleLabel"])
        stringField(group[key], `${path}.${key}`);
      if (typeof group.isActive !== "boolean")
        fail(`${path}.isActive`, "必须为布尔值");
      if (group.styleState !== "editorial" && group.styleState !== "uncertain")
        fail(`${path}.styleState`, "非法风格状态");
      if (!note(group.styleNote)) fail(`${path}.styleNote`, "非法说明文字");
      strings(group.aliases, `${path}.aliases`);
      strings(group.tags, `${path}.tags`);
      region(group.founding, `${path}.founding`);
      region(group.activity, `${path}.activity`);
      image(group.avatar, `${path}.avatar`);
      image(group.visual, `${path}.visual`);
      url(group.officialUrl, `${path}.officialUrl`);
      url(group.wikiUrl, `${path}.wikiUrl`);
      if (group.followers !== null && shape(group.followers, `${path}.followers`, [
        "display",
        "value",
        "observedAt"
      ])) {
        stringField(group.followers.display, `${path}.followers.display`);
        if (typeof group.followers.value !== "number" || !Number.isSafeInteger(group.followers.value) || group.followers.value < 0)
          fail(`${path}.followers.value`, "必须为非负安全整数");
        if (!observedAt(group.followers.observedAt))
          fail(`${path}.followers.observedAt`, "非法观察日期");
      }
      if (!Array.isArray(group.sources) || group.sources.length > 64)
        fail(`${path}.sources`, "必须为最多64项的来源数组");
      else
        Array.from(group.sources).forEach(
          (source, sourceIndex) => {
            const sp = `${path}.sources[${sourceIndex}]`;
            if (!shape(source, sp, ["label", "url", "observedAt", "note"]))
              return;
            stringField(source.label, `${sp}.label`, 400);
            url(source.url, `${sp}.url`, false);
            if (!observedAt(source.observedAt))
              fail(`${sp}.observedAt`, "非法观察日期");
            if (!note(source.note)) fail(`${sp}.note`, "非法来源说明");
          }
        );
    });
    return errors.length ? { valid: false, errors } : { valid: true, data: input, errors: [] };
  }
  function normalizeRegionName(value) {
    const normalized = value.normalize("NFKC").trim();
    const aliases = {
      广西壮族自治区: "广西",
      宁夏回族自治区: "宁夏",
      新疆维吾尔自治区: "新疆",
      内蒙古自治区: "内蒙古",
      西藏自治区: "西藏",
      香港特别行政区: "香港",
      澳门特别行政区: "澳门"
    };
    return Object.hasOwn(aliases, normalized) ? aliases[normalized] : normalized.length > 2 ? normalized.replace(/[省市]$/u, "") : normalized;
  }

  // src/catalog/runtime.ts
  function readCatalog() {
    if (typeof window === "undefined" || window.IDOL_GROUPS_DATA === void 0) {
      return { valid: false, errors: ["团体索引未加载，请检查资料脚本后重试。"] };
    }
    return validateCatalog(window.IDOL_GROUPS_DATA);
  }

  // src/events/model.ts
  var DATE_PATTERN = /^\d{4}-\d{2}-\d{2}$/;
  var TIME_PATTERN = /^(?:[01]\d|2[0-3]):[0-5]\d$/;
  var ID_PATTERN2 = /^e-[a-z0-9-]+$/;
  var UTC8_MS = 8 * 60 * 60 * 1e3;
  var DAY_MS = 24 * 60 * 60 * 1e3;
  var STATUS_LABELS = {
    scheduled: "按官宣计划",
    postponed: "已延期，请核实新日期",
    cancelled: "已取消",
    unconfirmed: "安排待确认"
  };
  function isObject(value) {
    return value !== null && typeof value === "object" && !Array.isArray(value);
  }
  function isDate(value) {
    if (typeof value !== "string" || !DATE_PATTERN.test(value)) return false;
    if (value.startsWith("0000-")) return false;
    const parsed = /* @__PURE__ */ new Date(`${value}T00:00:00.000Z`);
    return Number.isFinite(parsed.getTime()) && parsed.toISOString().slice(0, 10) === value;
  }
  function isTimestamp(value) {
    if (typeof value !== "string") return false;
    const match = /^(\d{4}-\d{2}-\d{2})T([01]\d|2[0-3]):([0-5]\d):([0-5]\d)(?:\.\d{1,9})?(Z|[+-](?:0\d|1[0-4]):[0-5]\d)$/.exec(
      value
    );
    if (!match || match[0] !== value || !isDate(match[1])) return false;
    const offset = match[5];
    if (offset && /^[+-]14:/.test(offset) && !offset.endsWith(":00"))
      return false;
    return Number.isFinite(Date.parse(value));
  }
  function isSafeUrl(value) {
    if (typeof value !== "string" || value.length > 2048) return false;
    if (!/^https?:\/\//i.test(value) || // eslint-disable-next-line no-control-regex -- 安全边界需要明确拒绝 URL 中的控制字符。
    /[\s\\\u0000-\u001f\u007f]|%0[ad]/i.test(value))
      return false;
    try {
      const url = new URL(value);
      return (url.protocol === "https:" || url.protocol === "http:") && Boolean(url.hostname) && !url.username && !url.password;
    } catch {
      return false;
    }
  }
  function isLocalEventPosterPath(value) {
    return typeof value === "string" && value.trim() === value && /^assets\/event-posters\/[a-z0-9][a-z0-9_-]{0,95}\.(?:png|jpe?g|webp)$/.test(
      value
    );
  }
  function validateEventDataset(input, knownGroupIds) {
    const errors = [];
    const knownIds = new Set(knownGroupIds);
    const ids = /* @__PURE__ */ new Set();
    const fail = (path, message) => {
      errors.push({ path, message });
    };
    const shape = (value, path, keys) => {
      if (!isObject(value)) {
        fail(path, "必须为对象");
        return false;
      }
      for (const key of keys) {
        if (!Object.prototype.hasOwnProperty.call(value, key))
          fail(`${path}.${key}`, "缺少必需字段");
      }
      for (const key of Object.keys(value)) {
        if (!keys.includes(key)) fail(`${path}.${key}`, "不接受未定义字段");
      }
      return true;
    };
    const text2 = (value, path, max, nullable = false, empty = false) => {
      if (nullable && value === null) return;
      if (typeof value !== "string" || !empty && !value.trim() || typeof value === "string" && [...value].length > max) {
        fail(
          path,
          `必须为${nullable ? " null 或" : ""}${empty ? "" : "非空"}字符串，最多 ${max} 字符`
        );
      }
    };
    if (!shape(input, "$", ["schemaVersion", "updatedAt", "coverage", "events"]))
      return { valid: false, errors };
    if (input.schemaVersion !== "idol-events-v1")
      fail("$.schemaVersion", "不支持的数据版本");
    if (input.coverage !== "partial")
      fail("$.coverage", "必须明确为 partial，不能声称完整覆盖");
    if (!isTimestamp(input.updatedAt))
      fail("$.updatedAt", "必须为含明确时区的有效 ISO 时间");
    if (!Array.isArray(input.events) || input.events.length > 1e4) {
      fail("$.events", "必须为最多 10000 条记录的数组");
      return { valid: false, errors };
    }
    Array.from(input.events).forEach((event, index) => {
      const p = `$.events[${index}]`;
      if (!shape(event, p, [
        "id",
        "title",
        "date",
        "province",
        "city",
        "venue",
        "address",
        "opensAt",
        "startsAt",
        "endsAt",
        "status",
        "performers",
        "sources",
        "notes",
        "poster"
      ]))
        return;
      if (typeof event.id !== "string" || event.id.trim() !== event.id || !ID_PATTERN2.test(event.id) || event.id.length > 100) {
        fail(`${p}.id`, "活动 ID 格式无效或超过 100 字符");
      } else if (ids.has(event.id)) {
        fail(`${p}.id`, "活动 ID 重复");
      } else {
        ids.add(event.id);
      }
      text2(event.title, `${p}.title`, 160);
      if (!isDate(event.date)) fail(`${p}.date`, "必须为真实日历日期 YYYY-MM-DD");
      for (const field of ["province", "city"]) {
        text2(event[field], `${p}.${field}`, 40, true);
        if (typeof event[field] === "string" && (event[field].trim() !== event[field] || !/^[\p{Script=Han}·]+$/u.test(event[field]))) {
          fail(`${p}.${field}`, "省市须使用中文名称；未知时填写 null");
        }
      }
      text2(event.venue, `${p}.venue`, 240, true);
      text2(event.address, `${p}.address`, 500, true);
      text2(event.notes, `${p}.notes`, 4e3, false, true);
      for (const field of ["opensAt", "startsAt", "endsAt"]) {
        if (event[field] !== null && (typeof event[field] !== "string" || event[field].trim() !== event[field] || !TIME_PATTERN.test(event[field]))) {
          fail(`${p}.${field}`, "必须为 HH:mm 或 null，不支持自行推断跨日时间");
        }
      }
      if (typeof event.opensAt === "string" && typeof event.startsAt === "string" && event.opensAt > event.startsAt) {
        fail(`${p}.opensAt`, "入场不能晚于同日开演；跨日信息请留空并备注");
      }
      if (typeof event.startsAt === "string" && typeof event.endsAt === "string" && event.endsAt <= event.startsAt) {
        fail(`${p}.endsAt`, "结束必须晚于同日开演；跨日信息请留空并备注");
      }
      if (typeof event.status !== "string" || !Object.prototype.hasOwnProperty.call(STATUS_LABELS, event.status)) {
        fail(`${p}.status`, "无效的活动状态");
      }
      if (!Array.isArray(event.performers) || event.performers.length > 200) {
        fail(`${p}.performers`, "必须为最多 200 项的数组");
      } else {
        Array.from(event.performers).forEach((performer, i) => {
          const pp = `${p}.performers[${i}]`;
          if (!shape(performer, pp, ["groupId", "name"])) return;
          text2(performer.name, `${pp}.name`, 160);
          if (performer.groupId !== null && (typeof performer.groupId !== "string" || !knownIds.has(performer.groupId))) {
            fail(
              `${pp}.groupId`,
              "团体 ID 必须来自当前完整主档；未绑定时填 null"
            );
          }
        });
      }
      if (!Array.isArray(event.sources) || !event.sources.length || event.sources.length > 30) {
        fail(`${p}.sources`, "必须有 1 至 30 项可追溯来源");
      } else {
        Array.from(event.sources).forEach((source, i) => {
          const sp = `${p}.sources[${i}]`;
          if (!shape(source, sp, [
            "url",
            "label",
            "publisher",
            "observedAt",
            "kind"
          ]))
            return;
          if (!isSafeUrl(source.url))
            fail(`${sp}.url`, "必须为安全的绝对 HTTP(S) URL");
          text2(source.label, `${sp}.label`, 240);
          text2(source.publisher, `${sp}.publisher`, 160);
          if (!isTimestamp(source.observedAt))
            fail(`${sp}.observedAt`, "必须为含明确时区的有效观察时间");
          if (typeof source.kind !== "string" || !["official", "organizer", "venue", "wiki"].includes(source.kind))
            fail(`${sp}.kind`, "来源种类无效");
        });
      }
      if (event.poster !== null && shape(event.poster, `${p}.poster`, ["src", "alt", "sourceUrl"])) {
        if (!isSafeUrl(event.poster.src) && !isLocalEventPosterPath(event.poster.src))
          fail(
            `${p}.poster.src`,
            "海报必须为安全 URL 或 assets/event-posters 下的 PNG/JPEG/WebP 图片"
          );
        text2(event.poster.alt, `${p}.poster.alt`, 240);
        if (!isSafeUrl(event.poster.sourceUrl))
          fail(`${p}.poster.sourceUrl`, "海报必须有安全来源 URL");
      }
    });
    if (errors.length) return { valid: false, errors };
    return { valid: true, data: input, errors: [] };
  }
  function filterEvents(events, filters) {
    const { from = "", to = "", group = "", province = [], city = [] } = filters;
    if (from && !isDate(from) || to && !isDate(to) || from && to && from > to)
      return [];
    const normalize = (value) => value.normalize("NFKC").toLowerCase();
    const terms = normalize(filters.q ?? "").trim().split(/\s+/).filter(Boolean);
    return events.filter((event) => {
      if (from && event.date < from || to && event.date > to) return false;
      if (group && !event.performers.some((performer) => performer.groupId === group))
        return false;
      if ((province.length || city.length) && !(event.province !== null && province.includes(event.province)) && !(event.city !== null && city.includes(event.city)))
        return false;
      const haystack = normalize(
        [
          event.title,
          event.province,
          event.city,
          event.venue,
          event.address,
          event.notes,
          ...event.performers.map((performer) => performer.name)
        ].filter((value) => value !== null).join(" ")
      );
      return terms.every((term) => haystack.includes(term));
    });
  }
  function getEventTemporalState(event, now) {
    if (!isDate(event.date) || !Number.isFinite(now.getTime()))
      throw new RangeError("日期或当前时间无效");
    const local = new Date(now.getTime() + UTC8_MS);
    if (!Number.isFinite(local.getTime()) || local.getUTCFullYear() < 1 || local.getUTCFullYear() > 9999) {
      throw new RangeError("当前时间超出可支持年份");
    }
    const today = local.toISOString().slice(0, 10);
    return event.date < today ? "past" : event.date > today ? "upcoming" : "today";
  }
  function escapeIcsText(value) {
    return value.replace(/\\/g, "\\\\").replace(/\r\n|\r|\n/g, "\\n").replace(/;/g, "\\;").replace(/,/g, "\\,").replace(/[\u0000-\u0008\u000b\u000c\u000e-\u001f\u007f]/g, "");
  }
  function foldIcsLine(value) {
    const encoder = new TextEncoder();
    let line = "";
    let bytes = 0;
    const lines = [];
    for (const character of value) {
      const length = encoder.encode(character).length;
      if (bytes + length > 75) {
        lines.push(line);
        line = " ";
        bytes = 1;
      }
      line += character;
      bytes += length;
    }
    lines.push(line);
    return lines.join("\r\n");
  }
  function icsTimestamp(date2) {
    if (!Number.isFinite(date2.getTime()) || date2.getUTCFullYear() < 1 || date2.getUTCFullYear() > 9999) {
      throw new RangeError("日历时间超出可支持年份");
    }
    return date2.toISOString().replace(/[-:]/g, "").replace(/\.\d{3}Z$/, "Z");
  }
  function buildCalendarIcs(events, generatedAt) {
    const stamp = icsTimestamp(generatedAt);
    const groupIds = /* @__PURE__ */ new Set();
    for (const event of events) {
      if (isObject(event) && Array.isArray(event.performers)) {
        for (const performer of event.performers) {
          if (isObject(performer) && typeof performer.groupId === "string")
            groupIds.add(performer.groupId);
        }
      }
    }
    const result = validateEventDataset(
      {
        schemaVersion: "idol-events-v1",
        updatedAt: generatedAt.toISOString(),
        coverage: "partial",
        events: [...events]
      },
      groupIds
    );
    if (!result.valid)
      throw new TypeError(
        result.errors.map((error) => `${error.path}: ${error.message}`).join("；")
      );
    const lines = [
      "BEGIN:VCALENDAR",
      "VERSION:2.0",
      "PRODID:-//China Underground Idol//Events v1//ZH-CN",
      "CALSCALE:GREGORIAN"
    ];
    for (const event of events) {
      lines.push(
        "BEGIN:VEVENT",
        `UID:${event.id}@idol-events.local`,
        `DTSTAMP:${stamp}`
      );
      if (event.startsAt === null) {
        const next = new Date(Date.parse(`${event.date}T00:00:00Z`) + DAY_MS);
        lines.push(`DTSTART;VALUE=DATE:${event.date.replace(/-/g, "")}`);
        if (next.getUTCFullYear() <= 9999)
          lines.push(
            `DTEND;VALUE=DATE:${next.toISOString().slice(0, 10).replace(/-/g, "")}`
          );
      } else {
        lines.push(
          `DTSTART:${icsTimestamp(/* @__PURE__ */ new Date(`${event.date}T${event.startsAt}:00+08:00`))}`
        );
        if (event.endsAt !== null)
          lines.push(
            `DTEND:${icsTimestamp(/* @__PURE__ */ new Date(`${event.date}T${event.endsAt}:00+08:00`))}`
          );
      }
      const prefix = event.status === "scheduled" ? "" : `【${STATUS_LABELS[event.status]}】`;
      lines.push(`SUMMARY:${escapeIcsText(prefix + event.title)}`);
      const description = [
        `状态：${STATUS_LABELS[event.status]}；日期经过不等于已实际举办。`,
        "中国当地时间 UTC+8；资料为部分收录，请在出发前核对官宣。",
        ...event.opensAt === null ? [] : [`入场：${event.opensAt}`],
        ...event.startsAt === null ? ["开演时间未确认，按全天日程导出。"] : [],
        ...event.performers.length ? [
          `已收录出演：${event.performers.map((performer) => performer.name).join("、")}`
        ] : [],
        event.notes,
        ...event.sources.map(
          (source2) => `来源：${source2.label}（${source2.publisher}，观察于 ${source2.observedAt}） ${source2.url}`
        )
      ].filter(Boolean).join("\n");
      lines.push(`DESCRIPTION:${escapeIcsText(description)}`);
      const location = [event.province, event.city, event.venue, event.address].filter(Boolean).join(" ");
      if (location) lines.push(`LOCATION:${escapeIcsText(location)}`);
      const source = event.sources[0];
      if (source) lines.push(`URL:${source.url}`);
      lines.push(
        `STATUS:${event.status === "cancelled" ? "CANCELLED" : event.status === "scheduled" ? "CONFIRMED" : "TENTATIVE"}`,
        "TRANSP:TRANSPARENT",
        "END:VEVENT"
      );
    }
    lines.push("END:VCALENDAR");
    return `${lines.map(foldIcsLine).join("\r\n")}\r
`;
  }

  // src/events/navigation.ts
  function emptyFilters() {
    return {
      q: "",
      from: "",
      to: "",
      group: "",
      province: [],
      city: [],
      period: "upcoming"
    };
  }
  function hasControlCharacters(value) {
    return Array.from(value).some((character) => {
      const code = character.charCodeAt(0);
      return code < 32 || code === 127;
    });
  }
  function isValidPageDate(value) {
    if (!/^(19|20|21)\d{2}-\d{2}-\d{2}$/u.test(value)) return false;
    if (value < "1900-01-01" || value > "2100-12-31") return false;
    const parsed = /* @__PURE__ */ new Date(`${value}T00:00:00Z`);
    return Number.isFinite(parsed.getTime()) && parsed.toISOString().slice(0, 10) === value;
  }
  function chinaToday(now = /* @__PURE__ */ new Date()) {
    return new Date(now.getTime() + 8 * 60 * 60 * 1e3).toISOString().slice(0, 10);
  }
  function weekendDates(now = /* @__PURE__ */ new Date()) {
    const date2 = /* @__PURE__ */ new Date(`${chinaToday(now)}T00:00:00Z`);
    const mondayOffset = (date2.getUTCDay() + 6) % 7;
    date2.setUTCDate(date2.getUTCDate() + 5 - mondayOffset);
    const from = date2.toISOString().slice(0, 10);
    date2.setUTCDate(date2.getUTCDate() + 1);
    return { from, to: date2.toISOString().slice(0, 10) };
  }
  function parseFilters(search, options) {
    const filters = emptyFilters();
    if (search.length > 8192) return { filters, ignored: true };
    const params = new URLSearchParams(search);
    let ignored = false;
    const single = (key, valid) => {
      const values = params.getAll(key);
      if (!values.length) return "";
      if (values.length !== 1 || !valid(values[0] ?? "")) {
        ignored = true;
        return "";
      }
      return (values[0] ?? "").trim();
    };
    filters.q = single(
      "q",
      (value) => value.length <= 120 && !hasControlCharacters(value)
    );
    filters.from = single(
      "from",
      (value) => value === "" || isValidPageDate(value)
    );
    filters.to = single("to", (value) => value === "" || isValidPageDate(value));
    filters.group = single(
      "group",
      (value) => value === "" || options.groupIds.has(value)
    );
    const multiple = (key, allowed) => {
      const values = params.getAll(key);
      if (values.length > 32) {
        ignored = true;
        return [];
      }
      const accepted = [];
      for (const value of values) {
        if (!value || value.length > 64 || hasControlCharacters(value)) {
          ignored = true;
          continue;
        }
        const match = [...allowed].find(
          (item) => normalizeRegionName(item) === normalizeRegionName(value)
        );
        if (match) accepted.push(match);
        else ignored = true;
      }
      return [...new Set(accepted)].sort();
    };
    filters.province = multiple("province", options.provinces);
    filters.city = multiple("city", options.cities);
    if (filters.from && filters.to && filters.from > filters.to) {
      filters.from = "";
      filters.to = "";
      ignored = true;
    }
    const period = single(
      "period",
      (value) => ["upcoming", "past", "all"].includes(value)
    );
    filters.period = period ? period : filters.from || filters.to ? "all" : "upcoming";
    for (const key of params.keys()) {
      if (!["q", "from", "to", "group", "province", "city", "period"].includes(key))
        ignored = true;
    }
    return { filters, ignored };
  }
  function serializeFilters(filters) {
    const params = new URLSearchParams();
    for (const key of ["q", "from", "to", "group"]) {
      if (filters[key]) params.set(key, filters[key]);
    }
    if (filters.period !== "upcoming" || filters.from || filters.to)
      params.set("period", filters.period);
    for (const key of ["province", "city"]) {
      for (const value of [...new Set(filters[key])].sort())
        params.append(key, value);
    }
    const query = params.toString();
    return query ? `?${query}` : "";
  }
  function selectPageEvents(events, filters, now = /* @__PURE__ */ new Date()) {
    const today = chinaToday(now);
    const regionValues = (key) => {
      const normalized = new Set(filters[key].map(normalizeRegionName));
      return [
        ...new Set(
          events.flatMap((event) => {
            const value = event[key];
            return value !== null && normalized.has(normalizeRegionName(value)) ? [value] : [];
          })
        )
      ];
    };
    const province = regionValues("province");
    const city = regionValues("city");
    if ((filters.province.length || filters.city.length) && !province.length && !city.length)
      return [];
    return filterEvents(events, { ...filters, province, city }).filter(
      (event) => filters.period === "all" || (filters.period === "past" ? event.date < today : event.date >= today)
    ).sort(
      (a, b) => a.date.localeCompare(b.date) || (a.startsAt || "99:99").localeCompare(b.startsAt || "99:99") || a.id.localeCompare(b.id)
    );
  }
  function resolveEventLocation(search, hash, options, events, now = /* @__PURE__ */ new Date()) {
    const parsed = parseFilters(search, options);
    let id = "";
    let invalidAnchor = false;
    try {
      if (hash.length > 256) invalidAnchor = true;
      else id = decodeURIComponent(hash.replace(/^#/u, ""));
    } catch {
      invalidAnchor = true;
    }
    const anchor = /^e-[a-z0-9-]+$/u.test(id) ? events.find((event) => event.id === id) ?? null : null;
    if (id.startsWith("e-") && !anchor) invalidAnchor = true;
    const adjusted = Boolean(
      anchor && !selectPageEvents(events, parsed.filters, now).some(
        (event) => event.id === anchor.id
      )
    );
    return {
      ...parsed,
      filters: adjusted ? { ...emptyFilters(), period: "all" } : parsed.filters,
      anchor,
      adjusted,
      invalidAnchor
    };
  }

  // src/events/page.ts
  var WEEKDAYS = ["一", "二", "三", "四", "五", "六", "日"];
  var STATUS_LABELS2 = {
    scheduled: "计划举行",
    postponed: "已延期 · 新安排请核对官宣",
    cancelled: "已取消",
    unconfirmed: "待确认 · 请核对官宣"
  };
  function monthDates(month) {
    if (!isValidPageDate(`${month}-01`)) return [];
    const start = /* @__PURE__ */ new Date(`${month}-01T00:00:00Z`);
    const offset = (start.getUTCDay() + 6) % 7;
    const count = new Date(
      Date.UTC(start.getUTCFullYear(), start.getUTCMonth() + 1, 0)
    ).getUTCDate();
    const dates = Array.from({ length: offset }, () => null);
    for (let day = 1; day <= count; day++)
      dates.push(`${month}-${String(day).padStart(2, "0")}`);
    while (dates.length % 7) dates.push(null);
    return dates;
  }
  function shiftMonth(month, offset) {
    if (!isValidPageDate(`${month}-01`)) return month;
    const date2 = /* @__PURE__ */ new Date(`${month}-01T00:00:00Z`);
    date2.setUTCMonth(date2.getUTCMonth() + offset);
    const shifted = date2.toISOString().slice(0, 7);
    return isValidPageDate(`${shifted}-01`) ? shifted : month;
  }
  function safeExternalUrl(value) {
    if (value.length > 2048 || !/^https?:\/\//iu.test(value) || hasControlCharacters(value) || /[\s\\]|%0[ad]/iu.test(value))
      return null;
    try {
      const url = new URL(value);
      return ["http:", "https:"].includes(url.protocol) && !url.username && !url.password ? url.href : null;
    } catch {
      return null;
    }
  }
  function safePosterUrl(value) {
    if (/^https?:\/\//iu.test(value)) return safeExternalUrl(value);
    return isLocalEventPosterPath(value) ? value : null;
  }
  function element(tag, text2 = "", className = "") {
    const node = document.createElement(tag);
    node.textContent = text2;
    if (className) node.className = className;
    return node;
  }
  function required(id) {
    const node = document.getElementById(id);
    if (!node) throw new Error(`缺少页面节点：${id}`);
    return node;
  }
  function externalLink(url, label) {
    const safe = safeExternalUrl(url);
    if (!safe) return element("span", `${label}（来源链接不可用）`);
    const link = element("a", label);
    link.href = safe;
    link.target = "_blank";
    link.rel = "noopener noreferrer";
    return link;
  }
  function uniqueRegions(values) {
    const regions = /* @__PURE__ */ new Map();
    for (const value of values) {
      const key = normalizeRegionName(value);
      if (!regions.has(key)) regions.set(key, value);
    }
    return [...regions.values()];
  }
  function eventEntry(event, now) {
    const article = element("article", "", "events-entry");
    article.id = event.id;
    article.tabIndex = -1;
    const date2 = element("time", "", "events-date");
    date2.dateTime = event.date;
    date2.append(
      element("strong", event.date.slice(5).replace("-", ".")),
      element("span", event.date.slice(0, 4))
    );
    const body = element("div");
    const badges = element("div", "", "events-badges");
    const status = element("span", STATUS_LABELS2[event.status], "events-badge");
    status.dataset.status = event.status;
    badges.append(status);
    const temporal = getEventTemporalState(event, now);
    if (temporal === "past")
      badges.append(element("span", "日期已过 · 不代表已举办", "events-badge"));
    else if (temporal === "today")
      badges.append(element("span", "今天", "events-badge"));
    body.append(badges, element("h3", event.title));
    body.append(
      element(
        "p",
        `${[event.province, event.city].filter(Boolean).join(" · ") || "地区待核实"} / ${event.venue || "场地待核实"}`
      )
    );
    body.append(
      element(
        "p",
        event.startsAt ? `开演 ${event.startsAt}（UTC+8）` : "开演时间未确认"
      )
    );
    const performers = element("div", "", "events-performers");
    performers.append(element("span", "出演："));
    if (!event.performers.length)
      performers.append(element("span", "出演阵容待核实"));
    for (const performer of event.performers) {
      if (performer.groupId) {
        const link = element("a", performer.name);
        link.href = `group.html?group=${encodeURIComponent(performer.groupId)}`;
        performers.append(link);
      } else performers.append(element("span", performer.name));
    }
    body.append(performers);
    const details = element("details", "", "events-detail");
    details.append(element("summary", "展开详情与官宣来源"));
    const detailBody = element("div", "", "events-detail-body");
    detailBody.append(
      element(
        "p",
        `入场：${event.opensAt || "待确认"} / 开演：${event.startsAt || "待确认"} / 结束：${event.endsAt || "待确认"}（UTC+8）`
      )
    );
    detailBody.append(
      element("p", `地址：${event.address || "待核实，请查阅官宣"}`)
    );
    if (event.notes) detailBody.append(element("p", event.notes, "events-notes"));
    if (event.status === "postponed")
      detailBody.append(
        element("p", "此处保留已收录日期；延期后的日期与安排请以最新官宣为准。")
      );
    const sources = element("ul");
    for (const source of event.sources) {
      const item = element("li");
      item.append(
        externalLink(source.url, `${source.label} · ${source.publisher}`)
      );
      item.append(
        element("span", ` · 观察时间：${source.observedAt}`, "events-meta")
      );
      sources.append(item);
    }
    detailBody.append(element("p", "资料来源"), sources);
    const correction = element("a", "补充或纠正这场活动");
    correction.href = `contribute.html?event=${encodeURIComponent(event.id)}&page=events&kind=event`;
    detailBody.append(correction);
    if (event.poster) {
      const src = safePosterUrl(event.poster.src);
      if (src) {
        const image = element("img", "", "events-poster");
        image.src = src;
        image.alt = event.poster.alt;
        image.loading = "lazy";
        image.width = 360;
        image.height = 480;
        image.addEventListener(
          "error",
          () => image.replaceWith(element("p", "海报暂时无法显示，请查看原始来源。")),
          { once: true }
        );
        detailBody.append(
          image,
          externalLink(event.poster.sourceUrl, "海报来源")
        );
      }
    }
    details.append(detailBody);
    body.append(details);
    article.append(date2, body);
    return article;
  }
  function mountEventsPage(dataset, groups, now) {
    const readNow = () => now ?? /* @__PURE__ */ new Date();
    let today = chinaToday(readNow());
    const form = required("events-filters");
    const query = required("events-query");
    const from = required("events-from");
    const to = required("events-to");
    const group = required("events-group");
    const periodInput = required("events-period");
    const cityShortcut = required("events-city-shortcut");
    const notice = required("events-filter-notice");
    const list = required("events-list");
    const status = required("events-result-status");
    const calendar = required("events-calendar");
    const grid = required("events-month-grid");
    const exportButton = required("events-export");
    const regionRoot = required("events-region-options");
    const options = {
      groupIds: new Set(groups.map((item) => item.id)),
      provinces: new Set(
        uniqueRegions(
          dataset.events.flatMap(
            (item) => item.province ? [item.province] : []
          )
        )
      ),
      cities: new Set(
        uniqueRegions(
          dataset.events.flatMap((item) => item.city ? [item.city] : [])
        )
      )
    };
    for (const city of [...options.cities].sort(
      (a, b) => a.localeCompare(b, "zh-CN")
    )) {
      const option = element("option", city);
      option.value = city;
      cityShortcut.append(option);
    }
    for (const item of [...groups].sort(
      (a, b) => a.name.localeCompare(b.name, "zh-CN")
    )) {
      const option = element("option", item.name);
      option.value = item.id;
      group.append(option);
    }
    const regions = /* @__PURE__ */ new Map();
    for (const event of dataset.events) {
      const province = [...options.provinces].find(
        (value) => event.province !== null && normalizeRegionName(value) === normalizeRegionName(event.province)
      ) || "省份待核实";
      if (!regions.has(province)) regions.set(province, /* @__PURE__ */ new Set());
      const city = [...options.cities].find(
        (value) => event.city !== null && normalizeRegionName(value) === normalizeRegionName(event.city)
      );
      if (city) regions.get(province)?.add(city);
    }
    const checkboxes = [];
    const checkbox = (name, value, label) => {
      const wrapper = element("label");
      const input = element("input");
      input.type = "checkbox";
      input.name = name;
      input.value = value;
      checkboxes.push(input);
      wrapper.append(input, document.createTextNode(label));
      return wrapper;
    };
    for (const [province, cities] of [...regions].sort(
      ([a], [b]) => a.localeCompare(b, "zh-CN")
    )) {
      if (!options.provinces.has(province) && !cities.size) continue;
      const fieldset = element("fieldset");
      fieldset.append(element("legend", province));
      if (options.provinces.has(province))
        fieldset.append(checkbox("province", province, `全部${province}`));
      for (const city of [...cities].sort())
        fieldset.append(checkbox("city", city, city));
      regionRoot.append(fieldset);
    }
    if (!regionRoot.childElementCount)
      regionRoot.append(
        element(
          "p",
          "暂无已核实地区，仍可按日期、团体或关键词筛选。",
          "events-help"
        )
      );
    required("events-updated").textContent = `资料更新时间：${dataset.updatedAt} · 收录范围：部分活动`;
    let filters = emptyFilters();
    let matches = [];
    let view = "list";
    let month = today.slice(0, 7);
    let selectedDate = "";
    const syncForm = () => {
      today = chinaToday(readNow());
      query.value = filters.q;
      from.value = filters.from;
      to.value = filters.to;
      group.value = filters.group;
      periodInput.value = filters.period;
      cityShortcut.value = filters.province.length || filters.city.length > 1 ? "__multiple__" : filters.city[0] || "";
      const customDates = Boolean(filters.from || filters.to);
      for (const button of form.querySelectorAll(
        "[data-period]"
      ))
        button.setAttribute(
          "aria-pressed",
          String(!customDates && button.dataset.period === filters.period)
        );
      to.setCustomValidity("");
      for (const input of checkboxes)
        input.checked = filters[input.name].includes(
          input.value
        );
      const count = filters.province.length + filters.city.length;
      required("events-region-count").textContent = count ? `已选 ${count} 项` : "不限地区";
      const advancedCount = Number(Boolean(filters.from || filters.to)) + Number(Boolean(filters.group)) + count;
      required("events-advanced-count").textContent = advancedCount ? `（${advancedCount}）` : "";
      required("events-active-filters").textContent = [
        filters.q ? `关键词：${filters.q}` : "",
        filters.from || filters.to ? `${filters.from || "不限开始"} 至 ${filters.to || "不限结束"}` : "",
        filters.group ? `团体：${groups.find((item) => item.id === filters.group)?.name || filters.group}` : "",
        ...filters.province,
        ...filters.city
      ].filter(Boolean).join(" · ");
    };
    const renderCalendar = () => {
      required("events-month-heading").textContent = `${month.slice(0, 4)} 年 ${Number(month.slice(5))} 月`;
      required("events-prev-month").disabled = month === "1900-01";
      required("events-next-month").disabled = month === "2100-12";
      grid.replaceChildren();
      for (const day of WEEKDAYS)
        grid.append(element("span", day, "events-weekday"));
      const counts = /* @__PURE__ */ new Map();
      for (const event of matches)
        counts.set(event.date, (counts.get(event.date) || 0) + 1);
      for (const date2 of monthDates(month)) {
        if (!date2) {
          const blank = element("span");
          blank.setAttribute("aria-hidden", "true");
          grid.append(blank);
          continue;
        }
        const count = counts.get(date2) || 0;
        const button = element("button", "", "events-day");
        button.type = "button";
        button.dataset.date = date2;
        button.dataset.hasEvents = String(count > 0);
        button.setAttribute("aria-label", `${date2}，${count} 场已收录活动`);
        button.setAttribute("aria-pressed", String(date2 === selectedDate));
        if (date2 === today) button.setAttribute("aria-current", "date");
        button.append(
          element("strong", String(Number(date2.slice(8)))),
          element("span", count ? `${count} 场` : "—")
        );
        button.addEventListener("click", () => {
          selectedDate = date2;
          render();
          grid.querySelector(`[data-date="${date2}"]`)?.focus();
        });
        grid.append(button);
      }
      required("events-show-month").hidden = !selectedDate;
    };
    const render = (at = readNow()) => {
      today = chinaToday(at);
      required("events-today").setAttribute(
        "aria-pressed",
        String(
          filters.period === "all" && filters.from === today && filters.to === today
        )
      );
      const weekend = weekendDates(at);
      required("events-weekend").setAttribute(
        "aria-pressed",
        String(
          filters.period === "all" && filters.from === weekend.from && filters.to === weekend.to
        )
      );
      matches = selectPageEvents(dataset.events, filters, at);
      const visible = view === "list" ? matches : matches.filter(
        (event) => selectedDate ? event.date === selectedDate : event.date.startsWith(month)
      );
      const rangeLabel = filters.from || filters.to ? "所选日期" : { upcoming: "近期", past: "历史", all: "全部日期" }[filters.period];
      status.textContent = `${rangeLabel} · 共 ${matches.length} 场已收录活动${view === "month" ? `，当前${selectedDate ? "日期" : "月份"} ${visible.length} 场` : ""}`;
      exportButton.disabled = matches.length === 0;
      exportButton.setAttribute(
        "aria-label",
        `下载当前筛选的 ${matches.length} 场活动 ICS`
      );
      calendar.hidden = view !== "month";
      required("events-list-view").setAttribute(
        "aria-pressed",
        String(view === "list")
      );
      required("events-month-view").setAttribute(
        "aria-pressed",
        String(view === "month")
      );
      required("events-list-caption").textContent = view === "month" ? selectedDate || `${month} 整月` : "按日期排列 · 状态以已收录官宣为准";
      if (view === "month") renderCalendar();
      list.replaceChildren(...visible.map((event) => eventEntry(event, at)));
      required("events-empty").hidden = visible.length > 0;
      required("events-empty-reason").textContent = matches.length && view === "month" ? "当前日期或月份暂无结果。可切换月份、显示整月，或切回日程查看全部筛选结果。" : "尚未收录不代表没有活动。试试其他日期或城市，也可补充可靠官宣。";
      required("events-empty-history").hidden = filters.period === "past" || !dataset.events.some((event) => event.date < today);
    };
    const resetMonth = () => {
      today = chinaToday(readNow());
      matches = selectPageEvents(dataset.events, filters, readNow());
      month = (filters.from || matches.find((event) => event.date >= today)?.date || matches[0]?.date || today).slice(0, 7);
      selectedDate = "";
    };
    const restore = () => {
      const parsed = resolveEventLocation(
        window.location.search,
        window.location.hash,
        options,
        dataset.events,
        readNow()
      );
      filters = parsed.filters;
      notice.textContent = [
        parsed.ignored ? "链接中部分筛选参数无效，已忽略；有效条件仍保留。" : "",
        parsed.adjusted ? "为显示链接中的活动，已切换至全部日期并清除冲突筛选。" : "",
        parsed.invalidAnchor ? "链接中的活动标识无效或尚未收录，仍可浏览下方日程。" : ""
      ].filter(Boolean).join(" ");
      if (parsed.anchor) view = "list";
      syncForm();
      resetMonth();
      render();
      if (parsed.anchor) {
        const target = document.getElementById(parsed.anchor.id);
        target?.querySelector("details")?.setAttribute("open", "");
        target?.focus({ preventScroll: true });
        target?.scrollIntoView({ block: "start" });
      }
    };
    const save = () => {
      const search = serializeFilters(filters);
      if (search !== window.location.search || window.location.hash) {
        try {
          window.history.pushState(
            null,
            "",
            `${window.location.pathname}${search}`
          );
        } catch {
          notice.textContent = "当前浏览器未允许更新地址，筛选仍可使用。";
        }
      }
      syncForm();
      resetMonth();
      render();
    };
    form.addEventListener("submit", (event) => {
      event.preventDefault();
      to.setCustomValidity(
        from.value && to.value && from.value > to.value ? "结束日期不能早于开始日期" : ""
      );
      if (!form.reportValidity()) return;
      const params = new URLSearchParams();
      for (const [key, value] of new FormData(form))
        if (typeof value === "string" && value) params.append(key, value);
      const parsed = parseFilters(params.toString(), options);
      filters = parsed.filters;
      notice.textContent = parsed.ignored ? "部分筛选内容无效，已忽略。" : "";
      save();
    });
    form.addEventListener("input", () => to.setCustomValidity(""));
    form.addEventListener("change", (event) => {
      if (event.target === cityShortcut || event.target === query) return;
      if (event.target === from || event.target === to) periodInput.value = "all";
      form.requestSubmit();
    });
    cityShortcut.addEventListener("change", () => {
      filters.city = cityShortcut.value ? [cityShortcut.value] : [];
      filters.province = [];
      notice.textContent = "";
      save();
    });
    const choosePeriod = (period) => {
      filters.period = period;
      filters.from = "";
      filters.to = "";
      view = "list";
      notice.textContent = "";
      save();
    };
    for (const button of form.querySelectorAll(
      "[data-period]"
    ))
      button.addEventListener(
        "click",
        () => choosePeriod(button.dataset.period)
      );
    required("events-empty-history").addEventListener(
      "click",
      () => choosePeriod("past")
    );
    const chooseDates = (range) => {
      filters = { ...filters, ...range, period: "all" };
      view = "list";
      notice.textContent = "";
      save();
    };
    required("events-today").addEventListener("click", () => {
      const date2 = chinaToday(readNow());
      chooseDates({ from: date2, to: date2 });
    });
    required("events-weekend").addEventListener(
      "click",
      () => chooseDates(weekendDates(readNow()))
    );
    query.addEventListener("search", () => form.requestSubmit());
    const reset = () => {
      filters = emptyFilters();
      view = "list";
      notice.textContent = "";
      save();
    };
    form.addEventListener("reset", (event) => {
      event.preventDefault();
      reset();
    });
    required("events-empty-reset").addEventListener("click", () => {
      reset();
      query.focus();
    });
    required("events-list-view").addEventListener("click", () => {
      view = "list";
      render();
    });
    required("events-month-view").addEventListener("click", () => {
      view = "month";
      render();
    });
    required("events-prev-month").addEventListener("click", () => {
      month = shiftMonth(month, -1);
      selectedDate = "";
      render();
    });
    required("events-next-month").addEventListener("click", () => {
      month = shiftMonth(month, 1);
      selectedDate = "";
      render();
    });
    required("events-current-month").addEventListener("click", () => {
      today = chinaToday(readNow());
      month = today.slice(0, 7);
      selectedDate = "";
      render();
    });
    required("events-show-month").addEventListener("click", () => {
      selectedDate = "";
      render();
      required("events-month-heading").focus();
    });
    window.addEventListener("popstate", restore);
    window.addEventListener("hashchange", restore);
    document.addEventListener("visibilitychange", () => {
      const at = readNow();
      if (!document.hidden && today !== chinaToday(at)) render(at);
    });
    exportButton.addEventListener("click", () => {
      const at = readNow();
      render(at);
      if (!matches.length) return;
      try {
        const content = buildCalendarIcs(matches, at);
        const url = URL.createObjectURL(
          new Blob([content], { type: "text/calendar;charset=utf-8" })
        );
        const link = element("a");
        link.href = url;
        link.download = `idol-events-${today}.ics`;
        document.body.append(link);
        link.click();
        link.remove();
        window.setTimeout(() => URL.revokeObjectURL(url), 6e4);
        status.textContent = `已生成 ${matches.length} 场活动的 ICS 文件，请手动导入日历应用。`;
      } catch {
        status.textContent = "日历文件生成失败，请重新筛选后再试。";
      }
    });
    restore();
  }
  function boot() {
    try {
      const catalog = readCatalog();
      if (!catalog.valid) throw new Error("团体索引缺失或无效");
      const validation = validateEventDataset(
        events_v1_default,
        catalog.data.groups.map((item) => item.id)
      );
      if (!validation.valid) throw new Error("活动资料校验失败");
      mountEventsPage(validation.data, catalog.data.groups);
    } catch {
      required("events-result-status").textContent = "活动资料暂时无法读取，请重新打开完整站点文件，或稍后重试。";
      required("events-export").disabled = true;
      for (const control of document.querySelectorAll("#events-filters input, #events-filters button, #events-filters select"))
        control.disabled = true;
    }
  }
  if (typeof document !== "undefined" && document.getElementById("events-app"))
    boot();
})();
